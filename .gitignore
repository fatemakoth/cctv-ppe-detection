# Model weights (auto-downloaded on first run)
*.pt
*.onnx

# Calibration (device-specific, each user runs their own)
calibration.json

# Runtime data
incidents.db
snapshots/

# Training runs (large, auto-generated)
runs/

# PPE model weights and dataset images (large files)
ppe_model/ppe_best.pt
ppe_model/train/
ppe_model/valid/
ppe_model/test/
ppe_model/ppe_train/

# Google Sheets credentials (never commit this)
credentials.json

# Python
__pycache__/
*.pyc
*.pyo
.env

# macOS
.DS_Store
